extern iostream;

int main() {
    nconst i64 total = 0;
    for (nconst i64 index = 0; index < 200000; index = index + 1) {
        total = total + ((index % 7) * 3) - (index % 5);
    }
    sys.stdout(total);
    return 0;
}
