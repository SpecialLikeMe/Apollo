#include <cstdint>
#include <iostream>

int main() {
    std::int64_t total = 0;
    for (std::int64_t index = 0; index < __LIMIT__; ++index) {
        total += ((index % 7) * 3) - (index % 5);
    }
    std::cout << total;
    return 0;
}