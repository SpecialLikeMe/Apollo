extern iostream

typedef opstruct run {
    asg("eval new ${name}") -> {
    }
    src("${name} do ${CODE}") -> {
        CODE
    }
}

int main() {
    str i = `sys.stdout("Hello, world!");`;
    eval new memem;
    memem do [i];
    return 0;
}