int loadBorrow(nconst i32& source) {
    nconst i32& borrow = &nconst source;
    return borrow;
}

int increment(nconst i32 value) {
    value = value + 1;
    return value;
}

int main() {
    nconst i32 value = 1;
    loadBorrow(value);
    value = increment(value);
    return value;
}