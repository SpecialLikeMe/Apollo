// This should compile: bypass blocks disable borrow and move checking for their contents.

int main() {
    str value = "ok";
    @bypass {
        str moved = value;
        sys.println(value);
        void* heap.alc(1) i32;
        void heap;
    }
    sys.println(value);
    return 0;
}