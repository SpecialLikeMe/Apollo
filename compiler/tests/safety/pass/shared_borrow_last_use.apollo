void inspect(const str& value) {
    sys.println(value);
    return;
}

i32 main() {
    nconst str value = "hello";
    const str& alias = &value;
    inspect(alias);
    value = "world";
    sys.println(value);
    return 0;
}