// This should fail: passing a non-copy value by value moves it into the callee.

void consume(str value) {
    return;
}

int main() {
    str message = "ok";
    consume(message);
    sys.println(message);
    return 0;
}
