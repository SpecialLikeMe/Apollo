void inspect(const i32& value) {
    sys.println(value);
    return;
}

int main() {
    nconst i32 value = 1;
    const i32& alias = &value;
    i32 branch = 0;
    if (branch) {
        inspect(alias);
    } else {
        value = 2;
    }
    return value;
}