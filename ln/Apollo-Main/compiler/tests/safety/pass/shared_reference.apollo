void inspect(const str& value) {
    sys.println(value);
    return;
}

int main() {
    str message = "ok";
    const str& alias = &message;
    inspect(alias);
    sys.println(alias);
    return 0;
}