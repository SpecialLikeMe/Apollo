extern iostream

typedef opstruct run {
    asg("eval ${NAME}") -> {}
    src("${NAME} do ${EXPR}") -> {EXPR}
}

int main() {
    sys.println("Hello, world");
    eval runner;
    runner do [sys.stdin()];
    return 0;
}