extern std io

typedef struct test {
    int id;
}

int main() {
    if (true) {
        global nconst unordered_map<i32, i32> mymap;
        global nconst vector<i32> myvec;
    }

    mymap[90] = 1;
    myvec[apnd] = 1;

    test t = {
        .id = 42
    };

    sys.printf("%i %i", mymap[90], myvec[0]);
    sys.printf("%i", t.id);
    return 0;
}