extern iostream;

fn<i32(i32)> xyz = clr-> [](i32 input) in let main::() =
    return input + 4;

int main() {
    i32 first = xyz(0);
    i32 second = xyz(5);
    sys.println("${first}${second}");
    return 0;
}