# Apollo examples

`readonly_alias.apollo` demonstrates read-only references as exclusive aliases with last-use release.

`mutable_reborrow.apollo` demonstrates passing an existing mutable reference through helper calls without breaking the borrow chain.