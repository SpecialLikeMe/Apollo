nconst str current = "sc1.png";
rdwindow render;

render.event(KEYPRESS_w) {
    current = "sc2.png";
}

int main() {
    render.show();
    while (true) {
        render.render("crr", current, 100, 100);
    }
    return 0;
}