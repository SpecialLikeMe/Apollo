memstruct Buffer {
    i32 size;
}

Buffer makeBuffer() {
    {
        nconst str arena = "arena_pool";
        Buffer value = Buffer{}.uses(arena);
        return value;
    }
}

int main() {
    return 0;
}