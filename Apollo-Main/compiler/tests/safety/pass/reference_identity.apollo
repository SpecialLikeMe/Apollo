// This should compile: returning a reference parameter is allowed.

str& identity(nconst str& value) {
    return value;
}

int main() {
    nconst str message = "ok";
    nconst str& alias = &nconst message;
    identity(alias);
    return 0;
}
