void inspect(const str& value) {
    sys.println(value);
    return;
}

int main() {
    str message = "ok";
    const str& alias = &message;
    inspect(&message);
    sys.println(message);
    return 0;
}