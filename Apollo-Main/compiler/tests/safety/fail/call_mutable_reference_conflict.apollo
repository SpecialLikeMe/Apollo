// This should fail: a shared borrow blocks passing the same binding to a mutable reference parameter.

void bump(nconst i32& value) {
    sys.println(value);
    return;
}

int main() {
    nconst i32 value = 1;
    const i32& alias = &value;
    bump(&nconst value);
    return alias;
}
