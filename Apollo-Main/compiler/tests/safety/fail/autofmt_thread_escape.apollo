class Box {
}

void consume(Box value) {
    return;
}

i32 main() {
    Box value = Box{};

    @autofmtdeclare {
        @induct value;
        join thread worker consume(value);
    }

    return 0;
}